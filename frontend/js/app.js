angular.module('app', [
  'ngRoute',
])
  .config (['$routeProvider'], ($routeProvider) => {
    // $routeProvider.when('/', {
    //   templateUrl: 'index.html',
    //   controller: 'mainController',
    // })
  })
